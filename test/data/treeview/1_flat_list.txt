# Sample file with routes that should build a single, flat RouteNode

       root GET          /                           home#index
            POST         /clients(.:format)          clients#create
       test GET          /test(.:format)             clients#show